x = 5
y = 3.14
z = "Hi"

# Dynamic Typing: Python is dynamically typed, so the same variable can store different data types during execution.

x = 10
x = "Now a string"

# Assigning Same Value: same value can be assigned to
# multiple variables in a single line.

a = b = c = 100
print(a, b, c)

# 4. Assigning Different Values: Multiple variables can also be
# assigned different values in a single line.


